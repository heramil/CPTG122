#include <iostream>
#include <queue>
using namespace std;

int main() {
    queue<int> que;

    que.push(100);
    que.push(200);
    que.push(300);

    cout << que.front() << endl;
    que.pop();
    cout << que.front() << endl;
    
    return 0;
}
