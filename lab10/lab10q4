#include <iostream>
#include <queue>
using namespace std;

int main() {
    queue<string> que;

    que.push("Alice");
    que.push("Bob");
    que.push("Charlie");

    cout << que.front() << endl;
    que.pop();
    cout << que.front() << endl;
    que.pop();
    cout << que.front() << endl;

    return 0;
}
